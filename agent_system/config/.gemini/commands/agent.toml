description = "Standard Agent Runner execution (Medium budget)"
prompt = """
I am delegating the execution of this plan to the Agent Runner.
!{python agent_system/core/run_agent_plan.py --planner gemini --executor codex --plan-file {{args}} --thinking-budget medium --task-summary "CLI Delegated: Standard"}
"""
