description = "VERBOSE: Plan with Claude (High Budget) -> Execute with Codex"
prompt = """
SYSTEM DIRECTIVE: You are acting as the Planning Agent.
PLANNER: Claude | BUDGET: High | EXECUTOR: Codex

TASK: {{args}}

GOAL: Minimal overhead. Perform the following steps autonomously:
1. Research the task surgicaly.
2. Write a detailed plan to 'agent_artifacts/gemini/temp/plan.md'.
3. Use the 'run_shell_command' tool to launch the runner in the background:
   '.venv/Scripts/python.exe agent_system/core/run_agent_plan.py --planner claude --thinking-budget high --task-summary "CLI Verbose: {{args}}" --plan-file agent_artifacts/gemini/temp/plan.md'
4. Provide a Delegation Receipt and EXIT immediately.
"""
