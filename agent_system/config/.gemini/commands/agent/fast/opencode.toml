description = "Fast execution using Local LLM (OpenCode) as the executor"
prompt = """
Executing low-budget plan via local OpenCode CLI.
!{python agent_system/core/run_agent_plan.py --planner gemini --executor opencode --thinking-budget low --plan-file {{args}}}
"""
