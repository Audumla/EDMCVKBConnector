# EDMCVKBConnector - Copilot Instructions

Python plugin for Elite Dangerous Market Connector that forwards game state to
VKB hardware.

## Repository Boundary

This repository contains the plugin only. The agent runner now lives in a
separate workspace.

- If a machine-local `AGENT.md` is present, it was generated by the external
  runner for this workspace. Follow it for artifact paths and runner commands.
- Do not add or depend on `agent_system/` or `scripts/agent_runners/` here.
- Keep machine-local runner artifacts out of tracked files.

## Preferred Change Areas

- `load.py`
- `src/edmcruleengine/`
- `data/`
- `docs/`
- `scripts/dev/`, `scripts/release/`, `scripts/validation/`
- `test/`

## Validation

Run the relevant plugin tests and keep changes scoped to the EDMC plugin.

<!-- agent-runner-start -->
<!-- Managed by AgentRunner. Re-run install.py to regenerate. -->

# GitHub Copilot Agent Guide

> Authoritative instruction file. Read before starting. Verify before marking any task complete.

## Agent Instruction Sync Policy

**This is one of several synced agent instruction files.**
All of the following must contain identical policies:
`AGENT.md` `AGENTS.md` `CLAUDE.md` `GEMINI.md` `.clinerules` `.github/copilot-instructions.md`

**When asked to add or change a rule for all agents**, do ONE of:
1. Edit all files listed above directly (update each one with the same change), OR
2. Update the AgentRunner runtime `agent_guide_generator.py` and re-run the installer for this workspace.

**When you modify any policy in this file**, apply the same change to all files above before marking the task complete.

## Provider Instruction Files

Authoritative instruction file per provider:
- `claude` -> `CLAUDE.md`
- `gemini` -> `GEMINI.md`
- `opencode` -> `AGENT.md` (shared guide)
- `codex` -> `AGENTS.md`
- `cline` -> `.clinerules`
- `copilot` -> `.github/copilot-instructions.md`
- `ollama` -> `AGENT.md` (shared guide)
- `llamacpp` -> `AGENT.md` (shared guide)
- `lmstudio` -> `AGENT.md` (shared guide)

## Artifact Output — HARD RULE

All agent-produced files MUST be written under the machine-local artifact root
documented in `AGENT.md`.

This includes:
- plans
- reports
- logs/transcripts
- intermediate batch/interrogator/unifier outputs

Canonical run directory format:
```
<artifact-root>/<agent-type>/reports/plan_runs/<YYYYMMDDTHHMMSSZ_slug>/
```

Never write agent outputs to:
- `batches/`
- `agent_artifacts/` (legacy path)
- `agent_runner/`
- project source folders
- any other non-artifact path

## Completion Requirements

Every task must end with ALL of the following before it is complete:

1. Smoke test passed — verify changed code compiles/runs without errors (see Smoke Test below)
2. Changelog recorded using the runtime `log_change.py` command documented in `AGENT.md`
3. Run outputs stored under the machine-local artifact root documented in `AGENT.md`
4. Delegations invoked using the runtime `run_agent_plan.py` command documented in `AGENT.md` (if applicable)

Tags (exact strings): `Bug Fix` | `Build / Packaging` | `Code Refactoring` | `Configuration Cleanup` | `Dependency Update` | `Documentation Update` | `New Feature` | `Performance Improvement` | `Test Update` | `UI Improvement`

Approved agent names: `claude` `gemini` `opencode` `codex` `cline` `copilot` `ollama` `llamacpp` `lmstudio`

## Smoke Test

After any major change run a quick smoke test to verify the project still compiles and has no syntax errors.
Fix all failures before recording the changelog or marking the task complete.

**Python** — syntax-check each changed file:
```
python -m py_compile path/to/changed_file.py
```
Or verify imports: `python -c "import <module_name>"`

**TypeScript / JavaScript**:
```
npx tsc --noEmit
node --check file.js
```

**Shell scripts**: `bash -n script.sh`

**General**: `<entry-point> --help` (or `--version`)

If a test suite exists, also run it: `python -m pytest tests/ -q` or the project-equivalent.

## Local LLM

For lightweight tasks (summarisation, changelog generation, quick code review) use the project-configured local LLM instead of consuming subscription quota.

Use the machine-local commands documented in `AGENT.md` for:
- status / start / stop
- one-shot queries
- per-project endpoint/model overrides
- local-model execution via `run_agent_plan.py`

## Artifact Layout

```
<artifact-root>/<agent-type>/reports/plan_runs/<YYYYMMDDTHHMMSSZ_slug>/
  plan_input.md   stdout.log   status.json   metadata.json
```

## Agent Delegation

Use the runtime `run_agent_plan.py` command documented in `AGENT.md`.

## Project Separation

Project source must not reference AgentRunner directories, runtime paths, or machine-local runner state.
Verify using the runtime `check_separation.py` command documented in `AGENT.md`.
<!-- agent-runner-end -->
